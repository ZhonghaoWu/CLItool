# CLItool
Build a CLI tool where users input tickers for a personal watchlist. The tool fetches current prices from a public API and prints summary stats. Add optional CSV export and allow refreshing the data with one command.
